(ns pallet.crate.mongodb-test
  (:use pallet.crate.mongodb)
  (:require
    [pallet.build-actions :as build-actions]
    [pallet.test-utils :as test-utils])
  (:use clojure.test))

; Now what?!
; Perhaps learning Clojure would be a sensible next step...
